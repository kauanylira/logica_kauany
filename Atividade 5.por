programa {
  funcao inicio() {
    inteiro real
    inteiro dolar
    inteiro total

    escreva("informe cotacao dolar:")
    leia(cotacao dolar)

    escreva("valor em real:")
    leia(real)

    //cauculo conversão do dolar em real
    total = real / dolar
    escreva(total)
  }
}
